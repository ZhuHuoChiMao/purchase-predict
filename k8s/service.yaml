apiVersion: v1
kind: Service
metadata:
  name: purchase-predict-api
spec:
  type: LoadBalancer
  selector:
    app: purchase-predict-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
